# Parallel Assistant Work

This file records focused changes made in parallel with Work so concurrent progress remains easy to audit.

## 2026-09-10

- Playback fix candidate: removed forced `crossOrigin="anonymous"` from the native `<video>` element because normal remote-video playback does not require CORS mode, and some hosts can fail when the browser requires CORS headers.
- Added Juju Factory (2007) and Route 66 - Ein amerikanischer albTraum (2004) to Needs Review as feature-length open-license candidates. Neither is public until a direct browser-compatible source is found and tested.
- Valkaama remains Needs Review because the known MKV/AC3 source is not broadly browser/iPhone compatible without a suitable rendition.
