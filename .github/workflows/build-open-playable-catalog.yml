name: Build playable open movie catalog

on:
  push:
    branches: [main]
    paths:
      - 'scripts/build-open-playable-fast.py'
      - '.github/workflows/build-open-playable-catalog.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-open-playable-catalog
  cancel-in-progress: true

jobs:
  discover-verify-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
      - name: Install ffmpeg
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
      - name: Discover and playback-check open feature films
        run: python scripts/build-open-playable-fast.py
      - name: Install pinned pnpm
        run: npm install --global pnpm@11.19.0
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Verify Cineyah invariants
        run: node scripts/verify-cineyah.mjs
      - name: TypeScript check
        run: pnpm exec tsc --noEmit
      - name: Verify static production export
        env:
          STATIC_EXPORT: 'true'
          NEXT_PUBLIC_SITE_ORIGIN: 'https://cineyah-movies.netlify.app'
        run: pnpm exec next build
      - name: Publish verified catalog to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/catalog.ts content/generated-playable.json content/playable-coverage.json
          if git diff --cached --quiet; then
            echo "No catalog changes to publish"
            exit 0
          fi
          git commit -m "feat: publish playback-verified open movie batch"
          git pull --rebase origin main
          git push origin HEAD:main
